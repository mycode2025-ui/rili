# 桌面卡片拖动区域修复（2026-09-07）

- 原问题：桌面卡片仅有标题文字附近约 24px 高的窄条可拖动，顶部和标题栏下半部属于无效区域。
- 修复：10 类桌面卡片的拖动区统一扩展到顶部左侧完整 48px 高区域，覆盖顶部留白、图标、标题及标题后的空白。
- 安全边界：拖动区在第一个操作按钮前结束，不覆盖新增、关闭、展开、置顶和设置按钮；右下缩放手柄保持独立。
- 指针回归：在每日一言窗口对原先无效的 `(4, 4)`、`(150, 44)` 派发真实按下/松开事件，均触发拖动；点击关闭按钮只触发关闭，不触发拖动。
- 验证：67 项测试、89 张视觉回归、Slint 编译检查和 Clippy 零警告通过。
- Release：`target/release/rili.exe`，SHA256 `F511A6C103716D1B7529E02B6AB6B5D5E4AB224E5BD30CE4363FD270FC703C4C`。隔离原生冒烟通过，程序已启动且进程响应正常。
